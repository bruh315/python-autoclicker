this is a python autoclicker (REQUIREMENTS: 
  -   make sure to have python installed
  -   make sure to have the python library pyautogui installed
  -   pip installed
  -   any os that will work if you will have python installed
)
